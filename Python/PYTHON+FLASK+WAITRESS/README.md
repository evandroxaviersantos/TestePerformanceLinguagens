#Instalação
Baixe do site official do pyton: https://www.python.org/downloads/windows/
Obs: Ao instalar o python, certifique que a versão é a mesma do firebird, python 32bits deve ser instalado para firebird 32bits, ou ambos 64;

py -m pip install flask
ou 
python -m pip install flask

py -m pip install fdb
ou 
python -m pip install fdb

py -m pip install json
ou 
python -m pip install json

py -m pip install waitress
ou 
python -m pip install waitress


#Executar
py -m apiFlask.py:app

#Referencias:
https://programminghistorian.org/en/lessons/creating-apis-with-python-and-flask
https://firebirdsql.org/file/documentation/drivers_documentation/python/fdb/getting-started.html
